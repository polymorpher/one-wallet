const base = process.env.PUBLIC_URL
const Paths = {
  root: base + '/',
  create: base + '/create',
  list: base + '/list',
  transfer: base + '/transfer',
  recover: base + '/recover',
}

export default Paths
